<!DOCTYPE html>
<html lang="en">
<%- include('partials/head'); %>

<body>
    <header>
        <%- include('partials/nav') %>
    </header>
    <main>
        <% if (data.length > 0) {%>
            <h1 class="display-5"><%=data[0].username %></h1>
            <p class="p-2"><%=data[0].bio %></p>
            <a href="/user/<%=data[0].id %>/edit"><button type="button" class="btn btn-secondary">Edit Profile</button></a>
            <a href="/user/<%=data[0].id %>/logout"><button type="button" class="btn btn-secondary">Log out</button></a>
        <%}%>

    </main>
<%- include('partials/footer') %>
</body>
</html>
